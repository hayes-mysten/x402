RESOURCE_SERVER_URL=http://localhost:4021
ENDPOINT_PATH=/weather
PRIVATE_KEY=
# Optional: specify network (defaults to base-sepolia if not set)
# For EVM networks: base-sepolia, base, ethereum, etc.
# For Sui networks: sui (mainnet) or sui-testnet
# NETWORK=base-sepolia
# 
# Note: For Sui networks, PRIVATE_KEY should be a base64-encoded Sui private key
# For EVM networks, PRIVATE_KEY should be a hex-encoded (with or without 0x prefix) private key 